import array;
import span;

fn main(): int {
    var arr: array::Array<span::Span> = array::Array();
    var s = span::make(0, 10);
    arr.push(s);
    var first = arr[0];
    return first.start + first.end;
}
