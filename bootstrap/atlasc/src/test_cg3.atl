import llvm_codegen.native;

fn make_project(): @resolver::Project {
    return null;
}

fn main(): int {
    var g = llvm_codegen.native::make_empty_codegen();
    var p = make_project();
    if p != null {
        llvm_codegen.native::generate(@g, p);
    }
    return 0;
}
