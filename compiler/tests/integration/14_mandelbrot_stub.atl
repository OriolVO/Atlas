// Exit Code: 0
// Output: *
extern "C" fn putchar(c: int32): int32;

fn main(): int {
    var count = 0;
    var x = 0;
    while x < 5 {
        count = count + x;
        x = x + 1;
    }
    if count == 10 {
        putchar(42i32);
        putchar(10i32);
    }
    return 0;
}
