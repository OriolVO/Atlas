import fs;
import io;
import array;

export fn main(): int {
    var arr: array::Array<char> = array::Array();
    if fs::read_file("test_fs.atl", @arr) {
        io::puts("Read successful\n");
    } else {
        io::puts("Read failed\n");
    }
    return 0;
}
